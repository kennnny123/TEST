const object = {
    firstName : "Vladyslav",
    lastName : "Balan",
    introduce : function() {
    return ('Hello, Im ' + object.firstName + ' ' + object.lastName + '!');
}
}
module.exports = object;
